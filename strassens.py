from matrix_operations import *


def strassens(a, b, crossover=2):
    # base case
    if length(a) == crossover:
        result = multiply_matrices(a, b)
    # recursive case
    else:
        # we need to write this part
        break
    return result
